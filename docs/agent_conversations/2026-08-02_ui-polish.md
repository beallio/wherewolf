# UI polish implementation round

- Date: 2026-08-02
- Objective: implement the approved `ui-polish` plan.
- Files modified: desktop actions, main window, SQL editor, schema/history widgets, settings service, and UI tests.
- Tests added: friendly translation targets, dataset selection, readable history, toolbar/action metadata, update preference, About content, and nullable schema columns.
- Design decisions: Qt's native toolbar extension handles narrow-width overflow after consolidating into one toolbar; history uses timestamp/query table columns; update checks are opt-in and no network path is started when disabled.
- Results: Ruff, ty, pytest, orchestration quality gates, and review-note integrity checks passed before the round-complete marker.

## Review round 01 follow-up

- Objective: resolve K1 and K2 from `ui-polish-review-01` without revisiting the eight verified plan items.
- Files modified: `desktop/main_window.py` and its UI regression tests; this session log.
- K1: the Edit menu now owns focus-routed Cut, Copy, Paste, and Select All actions. They walk from `QApplication.focusWidget()` to a supported parent, with result-table copy using `copy_selection()`. The editor keeps its widget-scoped actions for its own context menu.
- Regression test: with the Translation tab current and its `QPlainTextEdit` confirmed focused, a real Ctrl+C key event copies `TRANSLATED SQL HERE`; with the SQL editor confirmed focused, it copies `EDITOR SQL HERE`. The test also proves the Edit menu does not reuse the editor-owned Copy QAction.
- K2 negative controls: each was applied, confirmed in `git diff`, and run with the full `./run.sh uv run pytest` suite. Removing the friendly dialect map failed `test_main_window_translation_target_uses_friendly_dialect_names` (407 passed, 1 failed); putting the editor Copy QAction back in the Edit menu failed both focus-routing tests (406 passed, 2 failed); wrapping query controls in `QScrollArea` failed `test_main_window_query_controls_share_the_primary_toolbar_without_a_scroll_area` (407 passed, 1 failed); changing the update-check default to on failed `test_settings_service_update_check_defaults_off_and_round_trips` (407 passed, 1 failed); collapsing unknown nullable to `No` failed `test_schema_panel_displays_nullable_state_and_ordinal` (407 passed, 1 failed).
- Results: all mutations were restored immediately. The final gate run covers the restored state.
