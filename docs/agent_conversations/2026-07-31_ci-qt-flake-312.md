# Session Log: 2026-07-31 - Diagnose the flaky 3.12 Qt segfault in CI

- **date**: 2026-07-31
- **task objective**: Execute the `ci-qt-flake-312` investigation plan to measure the CI-only Python 3.12 Qt segfault and determine root cause, mitigation, or bounded dead end.
- **files modified**:
  - `docs/plans/2026-07-31_ci-qt-flake-312.md`
  - `docs/agent_conversations/2026-07-31_ci-qt-flake-312.md`
  - `.github/workflows/flake-probe.yml`
  - `.github/probe-mode`
- **tests added**: none
- **design decisions**:
  - Encoded probe mode in committed file `.github/probe-mode` read directly by `.github/workflows/flake-probe.yml` step `Resolve probe mode`, enabling non-default branch probe dispatches via `git push origin feat/ci-qt-flake-312`.
  - Restricted probe matrix to Python 3.12 (`10` parallel jobs) while narrowing.
  - Kept `timid = true` in `pyproject.toml` and test suite intact.
  - Did not implement fixes in Round 07; focused strictly on reporting directional narrowing data per review instructions.

- **results & hypothesis history**:
  - **Task 1 local baseline (3.12 + coverage, 20 runs)**: `0 / 20` crashes.
    - Command environment used: `UV_PROJECT_ENVIRONMENT=/tmp/wherewolf/.venv312`, `XDG_CACHE_HOME=/tmp/wherewolf/.cache`, `PYTHONPYCACHEPREFIX=/tmp/wherewolf/__pycache__`, `TMPDIR=/tmp/wherewolf`.
    - Local runs produced zero native crashes.

  - **Task 2 probe baseline runs (Python 3.12, 10 parallel jobs per run)**:
    - Probe run 1 (`30683425223`): `1 / 10` segfaults (1 ordinary failure).
    - Probe run 2 (`30683530788`): `1 / 10` segfaults (`0` ordinary failures).
    - Probe run 3 (`30683970309`): `4 / 10` segfaults (`0` ordinary failures).
    - Pooled 3.12 baseline (coverage + timid=true): `7 / 32 ≈ 21.9%` segfault rate across 2 historical CI runs and 30 probe runs.

  - **Task 4 probe narrowing dispatches (Round 07, Python 3.12, 10 jobs per run)**:
    - Dispatch 1: `no-cov` mode (`.github/probe-mode: no-cov`, run `30684555928`)
      - Segfaults: `0 / 10`
      - Ordinary failures: `0 / 10`
      - Unknown: `0 / 10`
      - Directional evidence: dropping coverage yielded 0 crashes in 10 jobs.
    - Dispatch 2: `deselect` mode (`.github/probe-mode: deselect`, run `30684612474`)
      - Segfaults: `0 / 10`
      - Ordinary failures: `1 / 10` (`FAILED tests/test_app.py::test_app_initialization` AppTest 3s timeout)
      - Unknown: `0 / 10`
      - Directional evidence: deselecting Qt widget tests (`tests/test_actions.py`, `tests/test_main_window.py`, `tests/test_catalog_dock.py`, `tests/test_sql_editor.py`) yielded 0 segfaults in 10 jobs.
    - Dispatch 3: `timid-false` mode (`.github/probe-mode: timid-false`, run `30684678655`)
      - Segfaults: `0 / 10`
      - Ordinary failures: `0 / 10`
      - Unknown: `0 / 10`
      - Directional evidence: running with `timid = false` yielded 0 segfaults in 10 jobs.

- **Task status at this point**:
  - Task 1: complete (`0 / 20` local 3.12 + coverage crashes).
  - Task 2: baseline 3.12 probe measured (`7 / 32 ≈ 21.9%` pooled baseline rate).
  - Task 3: skipped (no local reproduction).
  - Task 4: three narrowing dispatches completed (`no-cov`: `0/10`, `deselect`: `0/10`, `timid-false`: `0/10`).
  - B18 (`timid-false` verification): superseded by maintainer direction to test Python 3.14 deprecation path. Instrumentation remains in probe workflow (`2464e95`), but effective `timid` override was not executed via probe dispatch, leaving its effective behavior as an open question.
  - B19: matrix set to Python 3.14 with 20 parallel probe jobs, probe mode set to `baseline` (`timid = true`, coverage enabled, full test suite). Run `30685969710` completed: `segfaults 0 / 20 (ordinary 0, unknown 0)`.
  - B20: Python 3.14 confirmed clean across 20 samples (`0 / 20` segfaults, `0` ordinary, `0` unknown). Decision rule threshold satisfied (`(1 - 0.22)^20 ≈ 0.7%`). Next step per maintainer direction is Python 3.12 deprecation in subsequent round.

