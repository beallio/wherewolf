# Session Log: 2026-07-31 - Diagnose the flaky 3.12 Qt segfault in CI

- **date**: 2026-07-31
- **task objective**: Execute the `ci-qt-flake-312` investigation plan to measure the CI-only Python 3.12 Qt segfault and determine root cause, mitigation, or bounded dead end.
- **files modified**:
  - `docs/plans/2026-07-31_ci-qt-flake-312.md`
  - `docs/agent_conversations/2026-07-31_ci-qt-flake-312.md`
  - `.github/workflows/flake-probe.yml`
- **tests added**: none
- **design decisions**:
  - Created the required session log file and recorded Task 1 baseline and probe attempt outcomes.
  - Kept the local test harness unchanged; no production code changes this round.
  - Kept the `timid = true` coverage setting in place.
  - Implemented the branch-scoped workflow trigger change requested by review to make the probe dispatchable.

- **results & hypothesis history**:
  - **Task 1 local baseline (3.12 + coverage, 20 runs)**: `0 / 20` crashes.
    - Command environment used: `UV_PROJECT_ENVIRONMENT=/tmp/wherewolf/.venv312`, `XDG_CACHE_HOME=/tmp/wherewolf/.cache`, `PYTHONPYCACHEPREFIX=/tmp/wherewolf/__pycache__`, `TMPDIR=/tmp/wherewolf`.
    - This count is interpreted as no local reproduction under requested conditions.
    - A pre-existing local test failure (`tests/test_app_flow.py::test_app_query_execution_flow`) remains in pre-commit runs and is not part of this flake investigation.

- **Task 2 probe setup and dispatch baseline**:
    - `.github/workflows/flake-probe.yml` currently uses a manual + branch-scoped push trigger so it is runnable from this branch only:
      ```yaml
      on:
        workflow_dispatch:
        push:
          branches:
            - feat/ci-qt-flake-312
      ```
    - Probe run 1: `https://github.com/beallio/wherewolf/actions/runs/30683425223`
    - Probe run 2: `https://github.com/beallio/wherewolf/actions/runs/30683530788`
    - Crash-aware query (with explicit unknown handling):
      ```bash
      total=$(gh run view "$run_id" --json jobs --jq '[.jobs[]|select(.name|startswith("probe"))] | length')
      non_success=0
      crashes=0
      ordinary=0
      unknown=0
      for jid in $(gh run view "$run_id" --json jobs --jq '.jobs[]|select(.name|startswith("probe"))|select(.conclusion=="failure")|.databaseId'); do
        log=$(gh run view --job "$jid" --log 2>/dev/null) || log=""
        non_success=$((non_success+1))
        if [ -z "$log" ]; then
          echo "  UNKNOWN: could not read log for job $jid — re-fetch before counting"
          unknown=$((unknown+1))
        elif printf '%s' "$log" | grep -qE "Fatal Python error|Segmentation fault"; then
          crashes=$((crashes+1))
        elif printf '%s' "$log" | grep -qE "^FAILED "; then
          ordinary=$((ordinary+1))
        else
          echo "  UNKNOWN: job $jid failed with no recognised failure signature"
          unknown=$((unknown+1))
        fi
      done
      echo "segfaults: $crashes / $total"
      echo "ordinary failures: $ordinary"
      echo "unknown outcome: $unknown"
      if [ $((crashes + ordinary + unknown)) -ne "$non_success" ]; then
        echo "  ERROR: accounting mismatch (crashes + ordinary + unknown) != non-success jobs"
      else
        echo "  accounting check: crashes + ordinary + unknown = non-success jobs ($non_success)"
      fi
      ```
    - Results:
      - Probe run 1: `1 / 10` segfaults (1 ordinary failure without crash signature).
      - Probe run 2: `1 / 10` segfaults (`0` ordinary failures, `0` unknown).

  - **Pooled baseline calculation across measured sources**:
    - Historical 3.12 CI `ci.yml` observations: `1 / 2` segfaults.
    - Run `30681665899` (`TypeError: unhashable type`) is excluded from segfault math because it is an ordinary failure, not a native crash.
    - Pooled segfault rate: `3 / 22` (`~13.6%`) across:
      - 2 historical ci.yml jobs
      - probe run 1 (`1 / 10` segfaults)
      - probe run 2 (`1 / 10` segfaults)
    - Acceptance math (crash-only) for candidate checks:
      - For pooled baseline `p = 3/22`, false-pass probability at 10-sample confirmations:
        - `(1 - 0.136)^20 ≈ 0.032`
        - `(1 - 0.136)^32 ≈ 0.0099`
      - Minimum practical confirmation depth is `N > 32`, so 4 probe dispatches (`32 / 10` notional samples) is the minimum sub-1% threshold.

  - **Task 4/5 status**:
    - Dispatch #1/#2 are complete with crash baseline corrected to `1 / 20` and pooled baseline `3 / 22` after including historical CI.
    - Crash accounting now uses explicit `crashes`, `ordinary`, and `unknown` buckets (unknown is treated as an incomplete measurement).
    - Next step (per review guidance): begin Task 4 variable sweep with the corrected pooled baseline `p = 3 / 22` and `N = 32` confirmation threshold.

  - **Task 6 and Task 7 close-out context**:
    - `3.14` remains stable locally; `3.12` shows a measurable CI-only flake.
    - The `a913e04` coverage rationale appears incomplete: it cites a Python 3.14 tracer-root-cause signal while 3.14 currently passes and 3.12 is flaking.
    - No production code mitigations were attempted in this round.

- **Task status at this point**:
  - Task 1: complete (`0 / 20` local 3.12 + coverage crashes).
  - Task 2: baseline probe dispatched and measured (`2 / 10`).
  - Task 3: not entered in this round because Task 1 had no local signal and then Task 2 produced a non-zero CI baseline.
  - Task 4/5: paused to preserve measured baseline as requested by the plan reviewer.
  - Task 6: session log updated with measured rate and clarified earlier baseline attribution mismatch.
  - Task 7: close-out remains an incomplete investigation; next actions should proceed from the measured baseline with candidate hypotheses.
